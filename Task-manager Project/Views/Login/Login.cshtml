@model User
@{
	ViewData["Title"] = "Create New Account";
}
<div class="container d-flex justify-content-center align-items-center min-vh-100">
	<div class="card shadow-lg p-4 rounded-4" style="width:100%; max-width:450px;">
		<h2 class="text-center mb-3">Hello Again👋</h2>
		<p class="text-center text-muted  mb-4">Login</p>
		<form asp-action="Login">
			<div class="mb-3">
				<label asp-for="Email" class="form-label">E-Mail</label>
				<input asp-for="Email" class="form-control" name="email" placeholder="e.g: youremail@gmail.com" />
				<span asp-validation-for="Email" class="text-danger small"></span>
			</div>
			<div class="mb-3">
				<label asp-for="Password_hash" class="form-label">Enter Password</label>
				<input asp-for="Password_hash" type="password" name="password" class="form-control" placeholder="********" />
				<span asp-validation-for="Password_hash" class="text-danger small"></span>
			</div>
			<button type="submit" class="btn btn-primary w-100 mt-2">Login</button>
			<div class="text-center mt-3">
				<small class="text-muted">Don't Have An Account?<a asp-action="Create">Sign Up!</a></small>
			</div>
		</form>
	</div>
</div>